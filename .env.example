# Example environment variables
# For local development (Vite) - prefix client-exposed vars with VITE_
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# Server-side secrets (do NOT prefix with VITE_ and do NOT commit real values)
RECAPTCHA_SECRET=your_recaptcha_secret_here
SENDGRID_API_KEY=SG.xxxxx
DEST_EMAIL=you@example.com

# On Netlify: set these in Site settings -> Build & deploy -> Environment
# Note: Vite build will include VITE_ prefixed variables in the client bundle.
